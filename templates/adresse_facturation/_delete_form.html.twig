<form method="post" action="{{ path('app_adresse_facturation_delete', {'id': adresse_facturation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ adresse_facturation.id) }}">
    <button class="btn btn-primary">Supprimer cette adresse</button>
</form>
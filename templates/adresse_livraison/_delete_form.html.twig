<form method="post" action="{{ path('app_adresse_livraison_delete', {'id': adresse_livraison.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ adresse_livraison.id) }}">
    <button class="btn">Delete</button>
</form>
